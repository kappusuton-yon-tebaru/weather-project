FROM node:18-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

ARG NEXT_PUBLIC_GATEWAY_ENDPOINT
ENV NEXT_PUBLIC_GATEWAY_ENDPOINT ${NEXT_PUBLIC_GATEWAY_ENDPOINT}

RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]